IgeFilters.stroke = function (canvas, ctx, originalImage, texture, data) {
	// gStart = new IgePoint2d(x, y + ($TILESIZE);
	// gEnd = new IgePoint2d(gridMaxX, y + ($TILESIZE);

	
	// point1 = new IgePoint2d(0, canvas.height / 2);
	// point2 = new IgePoint2d(canvas.width, 0);

	// ctx.strokeStyle = data.color;
	// ctx.lineWidth = 1;
	// ctx.beginPath();
	// ctx.moveTo(point1.x, point1.y);
	// ctx.lineTo(point2.x, point2.y);
	// ctx.stroke();

	// ctx.strokeStyle = 'green';
	// ctx.lineWidth = 2;
	// ctx.beginPath();
	// ctx.moveTo(0, 0);
	// ctx.lineTo(0, 1);
	// ctx.stroke();
};